import { DateAgoQuesPipe } from './date-ago-ques.pipe';

describe('DateAgoQuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAgoQuesPipe();
    expect(pipe).toBeTruthy();
  });
});
