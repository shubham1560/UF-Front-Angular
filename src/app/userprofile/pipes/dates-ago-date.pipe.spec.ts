import { DatesAgoDatePipe } from './dates-ago-date.pipe';

describe('DatesAgoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DatesAgoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
